import { Component, Input } from '@angular/core';

@Component({
  selector: 'map-pin',
  templateUrl: 'map-pin.html',
  //inputs: ['image', 'x', 'y']
})
export class MapPinComponent {

    // @Input('image') image;
    // @Input('x') x;
    // @Input('y') y;
    //@Input('color') color;

  constructor() {
  }

}
